[project]
name = "nlr-ghe-screen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "geopandas>=1.0.1",
    "jsonschema>=4.25.1",
    "pytest",
    "requests>=2.32.5",
]

[project.scripts]
nlr-ghe-screen = "nlr_ghe_screen:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"